# Railway Configuration for Production Environment
# This file configures the production Railway service

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && npm install && npm start"

# Production environment variables
[env]
NODE_ENV = "production"

# This service will deploy from the 'main' branch
